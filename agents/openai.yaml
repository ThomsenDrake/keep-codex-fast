interface:
  display_name: "Keep Codex Fast"
  short_description: "Safe maintenance for Codex app/CLI/IDE local state"
  brand_color: "#22A6A6"
  default_prompt: "Use $keep-codex-fast to inspect my Codex app/CLI/IDE local state and recommend a safe maintenance plan."

policy:
  allow_implicit_invocation: true
